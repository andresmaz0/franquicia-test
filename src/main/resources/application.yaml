server:
  port: ${PORT:8080}
  servlet:
    context-path: /app-franquicias/api

spring:
  application:
    name: prueba-back
  servlet:
    multipart:
      max-file-size: 1MB
      max-request-size: 1MB
  config:
    import: "app-franquicias.yml"
